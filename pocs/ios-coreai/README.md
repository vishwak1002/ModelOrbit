# iOS Core AI POC

This POC will load only candidates admitted by the cross-platform preflight, run the shared canonical task, observe the no-network gate, and emit a validated evidence record with the exact iPhone and iOS manifest.
