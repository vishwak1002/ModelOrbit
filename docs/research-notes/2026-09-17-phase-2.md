# Phase 2 research note — 2026-09-17

## Question

Which exact snapshot revisions can run on both an iPhone and an Android phone using the Core AI and ExecuTorch lanes?

## Observation

The 26-row snapshot is reproducible, but contains no immutable Hugging Face revision values. The local host has Xcode 26.4.1 (17E202), xcrun 72, Swift 6.3.1, and iOS SDK 26.4. It has no ADB, Android SDK, Gradle, Java, or Kotlin compiler. `devicectl` did not list a physical iPhone. No Android device could be queried.

## Result

The preflight matrix has 52 explicit results. Unsupported pipeline families are `blocked`; records that cannot be checked without an immutable revision or unavailable Android toolchain are `unknown`. Both eligible counts are zero and the intersection status is `unknown`. This is not evidence that no model can run; it is evidence that the prerequisites did not establish an intersection on this host.

No model weights, credentials, raw network payloads, private device identifiers, or fabricated benchmark values entered the repository. The native harnesses compile or fail closed and the phase ledger records zero device runs.

## Reproduction

```text
npm test
npm run validate
npm run native:check
```

Artifacts: `data/preflight/2026-09-17.json`, `data/devices/2026-09-16.json`, and `evidence/runs/phase-2-status.json`.
