# Evidence schema

This package owns versioned JSON Schemas for model records, preflight results, device manifests, benchmark results, evidence links, and comparison route state.

Every producer validates its own output. The status reducer is the only component allowed to derive `cross-platform-ready`.
